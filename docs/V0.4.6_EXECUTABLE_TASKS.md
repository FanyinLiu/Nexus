# Nexus v0.4.6 执行计划：「形象运行时可靠性」

状态：`v0.4.6-beta.1` 已于 2026-08-10 通过受保护工作流发布为 GitHub
Pre-release。当前公开稳定版仍是 v0.4.5；多日真实使用验证窗口尚未关闭，
不得晋升为稳定版。

**主题**：让 Live2D 在透明 Electron 窗口、GPU 上下文重置、模型切换和失败
降级时都保持可预测。该版本是 0.4.x 的可靠性收口，不扩张到 v0.5 行为能力。

## 当前范围与完成状态

| 优先级 | 工作项 | 状态 | 验收 |
|---|---|---|---|
| P0 | 透明窗口直通 Alpha 合成 | 已完成 | Pixi `premultipliedAlpha: false`；macOS 白边回归用例 |
| P0 | Pixi 本地能力探测 CSP | 已完成 | renderer server 与 meta CSP 均允许 `connect-src data:`，不放宽其他指令 |
| P0 | WebGL 上下文丢失恢复 | 已完成 | 独占 Canvas/App/Model 有界重建；连续最多两次；首帧后重置预算 |
| P0 | 失败可读降级 | 已完成 | 五语言提示替代可见技术错误；窗口仍可交互；诊断详情保留 |
| P0 | 三模型恢复烟测 | 已完成 | Mao/Haru/Hiyori 冷启动与切换；强制 context loss；恢复后单画布 |
| P0 | 打包与持续运行门禁 | 验证中 | `verify:release`、packaged smoke、sustained runtime、prerelease gate |
| P1 | 正式稳定晋升 | 未开始 | beta 验证窗口关闭后单独准备 `v0.4.6`，不得在本候选中提前声称 |

## 设计约束

- 不升级依赖；继续使用现有 Pixi、pixi-live2d-display 与 Electron 版本。
- 不引入第二套 Live2D 所有权模型；恢复仍走现有幂等销毁和晚到资源处理。
- 不新增遥测、桌面感知、存储迁移或外部服务。
- 浏览器视觉烟测与 packaged Electron 门禁互补，任何一项都不能替代另一项。
- 临时报告与打包目录在完成审计后删除；长期行为测试和现有烟测脚本保留。

## 发布序列

1. `0.4.6-beta.1`：透明合成、CSP、上下文恢复、五语言降级、视觉/打包证据。
2. beta 验证：三平台 CI、真实机器反馈、崩溃/白边/恢复问题收敛。
3. `0.4.6` stable：只吸收 beta 缺陷修复和发布材料更新，不新增行为范围。

## 下一版本边界

- **v0.4.7 — Avatar Compatibility & Diagnostics**：导入前 Cubism 模型兼容性
  报告、资源/动作/表情缺口诊断、用户可操作的修复建议，以及打包后的形象
  切换诊断。它仍是 0.4.x 的兼容与维护版本。
- **v0.5.0 — Adaptive Desktop Presence**：把现有 sprite locomotion 扩展为
  sprite/Live2D 统一行为层，加入严格权限下的鼠标趋近、粗粒度打字节奏、空闲
  与窗口边缘反应；输入只保留派生信号，不记录内容。实体设备唤醒、最近扬声器
  路由和家庭设备控制不属于 0.5 基线，需另行完成设备发现、授权与隐私设计。

---

## 历史记录：原「记忆可信度」计划

状态：已提前发布（2026-08-06 更新）。原计划主题（P0 矛盾检测 + 搭车 F
迁移默认化）已随 **v0.4.5 stable** 发布，因此不再占用 0.4.6 版本号。以下
内容按原决策保留，作为历史记录。

**原主题**：把记忆系统从"能跑"变成"可信"。原计划交付对话层可感知的
记忆准确性，并坚持本地优先与非展示型 UI 原则。

## 现状盘点（2026-08-03 代码级勘察）

| 能力 | 现状 |
|---|---|
| on-this-day 日常唤起 | ✅ 已实现（useRelationshipState + ledger 去重 + prompt 注入） |
| 对话记忆唤起 recall | ✅ 已实现（keyword/hybrid/vector + emotion 加权，注入 assistantReply） |
| 记忆整合（聚类/合并/冷归档/反思去重） | ✅ 已实现（useMemoryDream 第 1–6 步） |
| 归档导入导出 | ✅ 记忆面板已有 |
| **矛盾检测** | ❌ 不存在 |
| **整理过程可见性** | ❌ 合并/归档零感知，无记录无撤销 |
| **唤起 trace 展示** | ⚠️ recallTrace 构建函数存在，未接入 UI |

## 范围

### P0 — 矛盾检测（新能力，ackem contradictionDetector 参照）【已完成】

- [x] `src/features/memory/contradictionDetector.ts`（纯函数，可测）：
  - `rankContradictionCandidates(memories, similarityFn)`：仅对新写入记忆 × 旧记忆做两两语义相似度（注入式，测试可替），过滤同簇（relatedIds）、已 superseded、自身对；排序 + top 20 截断。
  - `judgeContradictionPair(a, b, similarity)`：三档——`likely`（相似 ≥ 0.85 且情感极性相反）/ `possible`（相似 ≥ 0.8）/ `none`。
  - `applyContradictionResolutions(memories, resolutions)`：落地为 MemoryItem 新字段 `supersededBy` / `supersededAt` / `supersededPending`；幂等；已确认不覆盖，pending 可升级。
  - `detectContradictions(newMemories, existingMemories, deps)`：异步编排（embed 新记忆一次 + keyword 预筛 top 8 旧记忆再 embed，限定成本）。
- [x] MemoryItem 类型 + `normalizeMemoryItemsForStorage` 白名单加新字段（已确认白名单裁剪行为并测试覆盖）。
- [x] dream 集成：useMemoryDream 在 decay 后、聚类前跑检测（现有 `embedMemorySearchText`，无额外 LLM 成本）；失败降级为 debug 事件不阻塞 dream。
- [x] recall 降权：`rankMemories`（keyword 路径）+ `scoreItem`（vector/hybrid 路径）。
- **两档全自动（2026-08-03 决策：砍掉 P1 确认 UI）**：`likely` → ×0.3（新立场赢）；`possible` → ×0.6（温和防误伤）；零用户交互、零新 UI。`supersededPending` 语义 = 轻度降权标记（调试用）。
- **验收**：✅ 22 个新测试；✅ 全量 3036 测试全绿；✅ 无新感知来源；✅ `verify:pr` 全绿（EXIT 0）。

### ~~P1 — 整理记录可见性~~【已砍，2026-08-03】

决策：P1（整理记录列表 + 矛盾确认 UI + 归档恢复）是**展示型 UI**，与已确立原则冲突（用户不看、低频、循环依赖——possible 档的意义依赖确认 UI）。改为两档全自动降权（见 P0），零 UI。

### P2（可选）— 唤起 trace 展示

- [ ] recallTrace 接入对话记录展示（"这条回复用了这些记忆"）。
- 可砍；视 beta 窗口定。

### 搭车 — 记忆迁移默认化（F）【已完成】

- [x] 核实：chat/companion 迁移亦为 flag 门控（`VITE_NEXUS_ENABLE_LOCAL_DATA_CHAT_*` / `..._COMPANION_*`），非默认；本版本只默认化**记忆域**，其他域保持门控（一次一域，观察反馈后推进）。
- [x] `isMemoryLocalDataMigrationFeatureEnabled` / `isMemoryLocalDataMigrationUiEnabled` 改为**默认开**：未设置 env 即启用；`...=0` 保留为紧急断路器；运行时 consent（`MEMORY_LOCAL_DATA_AUTHORITY_CONSENT_KEY`）仍是唯一用户控制——未授权不迁移。
- [x] 测试适配（flag 测试改为 default-on + 显式 opt-out 语义）。
- **回滚矩阵**：
  1. 构建期：`VITE_NEXUS_ENABLE_LOCAL_DATA_MEMORY_MIGRATION=0`（或 `_UI=0`）恢复隐藏/禁用，无需代码变更。
  2. 运行期：面板内「回滚」按钮 → `localDataRollbackMemoryMigration`（audit 记录 `memory-migration-rolled-back`，删除 SQLite 记录）。
  3. 数据：迁移前自动备份（backup envelope）；迁移后 localStorage 源数据保留至用户手动清理。
  4. consent 可随时关闭（`authority_disable`），关闭后不再同步/读取。
- **验收**：✅ 3036 测试全绿；✅ verify:pr 全绿；✅ 无新感知来源、无新 UI 文案（复用既有 i18n 键）。

## 里程碑

> 2026-08-06 注：P0 与搭车 F 已随 v0.4.5 stable 提前发布，M1/M3 作废；下表仅为历史记录。P2 仍为可选后续项。

| 阶段 | 内容 | 门禁 |
|---|---|---|
| ~~M1（0.4.6-beta.1）~~【作废】 | P0 完整（检测+降权+dream 集成）→ 已随 v0.4.5 发布 | verify:pr 全绿、记忆测试全绿、隐私审计（无新感知源） |
| ~~M2（0.4.6-beta.2 如有）~~【已砍】 | P1 整理记录 + 恢复归档 | i18n 5 语言、settings surface 审计 |
| ~~M3（0.4.6 stable）~~【作废】 | P2 可选 + F 迁移默认化 + beta 反馈修复 → F 已随 v0.4.5 发布 | prerelease-check 30 项、三平台 CI、打包 smoke |

## 风险与对策

| 风险 | 对策 |
|---|---|
| 误判矛盾（语义损失） | 保守阈值；`likely` 才自动降权；`possible` 仅标记交用户；不删除任何记忆 |
| 存储归一化裁剪新字段 | 先查 normalizeMemoryItemsForStorage 白名单，测试覆盖 |
| O(n²) embedding 开销 | 仅新记忆 × 旧记忆；候选对数上限；复用 embedding 缓存 |
| 降权影响既有 recall 语义 | 降权系数保守（0.3）+ 现有 recall 测试全绿为门禁 |

## 已放弃方向（记录）

- **年度回顾设置页 UI**（曾实现后回滚）：低频、空态常态、无动作闭环；价值主体是创造者（作者），非终端用户。数据层 `yearbookAggregator`/`yearbookRender`（仓库既有资产）保留未动。
- 周信情绪段落（D）、子 Agent 异步任务（E）：用户明确排除。
- 展示型数据统计/可视化：一律不做（作者向）。
