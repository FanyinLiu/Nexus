import { defaultThemeTokens } from '../tokens'
import type { ThemeDefinition } from '../../../types/theme'

export const systemDayTheme: ThemeDefinition = {
  id: 'system-day',
  name: 'System Day',
  description: 'Daylight companion glass theme aligned with the panel UI.',
  tokens: defaultThemeTokens,
}
